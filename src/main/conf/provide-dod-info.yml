provide-dod-info:
  #  out_dir: $ the directory where the ouput-files (marc.xml, txt, xlsl) will be placed
  #  corpus_orig_dir: $ the directory where the (original) corpus files are placed
  # alma_sru_search: $ the basic part of the Alma SRU search URI
  # cut_year: $ barcodes containing publish dates older than this value will be handled (140 years -> in 2021 value must be 1881)
  # out_file_name: $ the excel file with data extracted from Alma


